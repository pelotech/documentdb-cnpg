extends: default

# Helm templates are Go templates, not parseable YAML.
ignore: |
  charts/*/templates/

rules:
  line-length: disable
  document-start: disable
  truthy:
    check-keys: false
  braces:
    max-spaces-inside: 1
  comments:
    min-spaces-from-content: 1

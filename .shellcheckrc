# versions.env and build/<artifact>/pins.env are sourced at runtime but not available to
# the linter; the pins path is artifact-dynamic (build/${ARTIFACT}/pins.env), so also SC1090.
disable=SC1091,SC1090

apiVersion: v2
name: documentdb-gw
description: MongoDB-wire DocumentDB gateway for CloudNativePG, with an optional
  ImageVolume cluster
type: application
version: 0.1.1
appVersion: "0.114.0-0.1.3" # gateway image tag; bump when adopting a newer gateway build
kubeVersion: ">=1.35.0-0" # ImageVolume feature gate (only needed when cluster.create=true)
home: https://github.com/pelotech/documentdb-cnpg
sources:
  - https://github.com/pelotech/documentdb-cnpg
maintainers:
  - name: pelotech
